version: 0.2

env:
  variables:
    GO_VERSION: 1.21
    PACKAGE: "github.com/andrewkandzuba/openexchange-otel-go"

phases:
  install:
    runtime-versions:
      golang: $GO_VERSION
    commands:
      - echo CODEBUILD_SRC_DIR - $CODEBUILD_SRC_DIR
#      - echo GOPATH - $GOPATH
#
#      - echo Create dir in GOPATH for sources
#      - mkdir -p ${GOPATH}/src/${PACKAGE}
#
#      - echo Copy source files into GOPATH
#      - echo cp -a ${CODEBUILD_SRC_DIR}/.  ${GOPATH}/src/${PACKAGE}
#      - cp -a ${CODEBUILD_SRC_DIR}/.  ${GOPATH}/src/${PACKAGE}
#
#      - cd ${GOPATH}/src/${PACKAGE} && go get ./...
  build:
    commands:
#      - devops/build.sh
      - echo CODEBUILD_SRC_DIR - $CODEBUILD_SRC_DIR

#artifacts:
#  files:
#    - application